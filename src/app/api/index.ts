import { DRIVER_API } from './driver.api';
import { MEETING_API } from './meeting.api';
import { SESSION_API } from './session.api';

export class API {
  static readonly DRIVER = DRIVER_API;
  static readonly MEETING = MEETING_API;
  static readonly SESSION = SESSION_API;
}
